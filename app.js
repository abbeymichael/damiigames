/**
 * Shared Hosting & Production Entry Point for DAMII.
 *
 * Alternate entry point for hosting providers (e.g. cPanel / Passenger)
 * configured to use `app.js` as the Application Startup File.
 *
 * Usage:
 *   node app.js
 */

import "./scripts/start.js";
